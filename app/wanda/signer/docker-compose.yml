services:
  wanda-signer:
    build: .
    container_name: wanda-signer
    restart: unless-stopped
    ports:
      - "8787:8787"   # 用 NPM 反代到这个端口,加 SSL
    environment:
      - PORT=8787
      # - AUTH_KEY=换成随机串(可选,Loon 的 wanda_signer_url 末尾带 ?key=同值)
